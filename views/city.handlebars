<h1>Cool friends from your city {{city}}.</h1>
<div id="signers-names">
    {{#citizens}}
    <h4>{{#if this.url}}
        <a href="{{url}}">{{/if}}
        {{first}} {{last}}
        {{#if this.url}}</a>
        {{/if}}, {{age}}, 
        <a href="/signers/{{city}}">{{city}}</a>
    </h4>
    {{/citizens}}
</div>